# ffmpeg audio stream decoder
# 安装FFmpeg环境
    brew install ffmpeg

# 解码后的pcm文件是单声道，16000采样率，文件存储在data目录下
  原始mp3的采样率是44100 Hz, 转码后的采样率是16000 Hz

# 解码后的pcm文件可以使用 Audacity 播放器播放

新增非流式解码，通过feed小段数据进行解码；

一次性读取
true_size=128811

read_buffer true_size=32768 xxxxx
read_buffer true_size=32768 xxxxx
read_buffer true_size=32768 xxxxx
read_buffer true_size=30507 xxxxx
